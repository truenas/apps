{% from "macros/nginx.conf" import nginx_config %}
{% set tpl = ix_lib.base.render.Render(values) %}

{# {% if values.wger.postgres_image_selector == "postgres_17_image" %}
  {% do tpl.notes.add_deprecation("Postgres 17 is deprecated and will be removed in the near future. Please upgrade.") %}
{% endif %} #}

{% if not values.wger.trusted_origins %}
  {% do tpl.funcs.fail("At least one trusted origin must be specified") %}
{% endif %}

{% set wger_net = tpl.networks.create_internal("wger-net") %}

{% set nginx = tpl.add_container(values.consts.nginx_container_name, "nginx_image") %}
{% set server = tpl.add_container(values.consts.server_container_name, "image") %}
{% set worker = tpl.add_container(values.consts.worker_container_name, "image") %}
{% set beat = tpl.add_container(values.consts.beat_container_name, "image") %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perms_config = {"uid": values.consts.run_as_user, "gid": values.consts.run_as_group, "mode": "check"} %}

{# -- Postgres -- #}
{% set pg_config = {
  "user": values.consts.db_user,
  "password": values.wger.db_password,
  "database": values.consts.db_name,
  "volume": values.storage.postgres_data,
  "additional_options": {"wal_level": "logical"},
} %}
{% set postgres = tpl.deps.postgres(values.consts.postgres_container_name, values.wger.postgres_image_selector, pg_config, perm_container) %}
{% do postgres.container.add_network(wger_net) %}

{# -- Redis -- #}
{% set redis_config = {
  "password": values.wger.redis_password,
  "volume": {"type": "temporary", "volume_config": {"volume_name": "redis-data"}},
} %}
{% set redis = tpl.deps.redis(values.consts.redis_container_name, "redis_image", redis_config, perm_container) %}
{% do redis.container.add_network(wger_net) %}

{% set containers = [server, worker, beat] %}
{% for c in containers + [nginx] %}
  {% do c.add_network(wger_net) %}
  {% do c.set_user(values.consts.run_as_user, values.consts.run_as_group) %}
{% endfor %}
{% set celery_password = tpl.funcs.secure_string(32) %}
{% set origins = namespace(x=[]) %}
{% for origin in values.wger.trusted_origins %}
  {% do origins.x.append(origin.rstrip("/")) %}
{% endfor %}

{# -- Common Environment Variables -- #}
{% for c in containers %}
  {% do c.environment.add_env("SECRET_KEY", values.wger.secret_key) %}
  {% do c.environment.add_env("JWT_PRIVATE_KEY", values.wger.jwt_private_key) %}
  {% do c.environment.add_env("JWT_PUBLIC_KEY", values.wger.jwt_public_key) %}
  {% do c.environment.add_env("SITE_URL", values.wger.site_url.rstrip("/")) %}
  {% do c.environment.add_env("REFRESH_TOKEN_LIFETIME", values.wger.refresh_token_lifetime) %}
  {% do c.environment.add_env("TIME_ZONE", values.TZ) %}
  {% do c.environment.add_env("WGER_USE_GUNICORN", "True") %}
  {% do c.environment.add_env("DJANGO_DEBUG", "False") %}
  {% do c.environment.add_env("USE_CELERY", "True") %}
  {% do c.environment.add_env("CELERY_BROKER", "%s/2" | format(redis.get_url("redis"))) %}
  {% do c.environment.add_env("CELERY_BACKEND", "%s/2" | format(redis.get_url("redis"))) %}
  {% do c.environment.add_env("CELERY_FLOWER_PASSWORD", celery_password) %}
  {% do c.environment.add_env("DJANGO_DB_ENGINE", "django.db.backends.postgresql") %}
  {% do c.environment.add_env("DJANGO_DB_DATABASE", values.consts.db_name) %}
  {% do c.environment.add_env("DJANGO_DB_USER", values.consts.db_user) %}
  {% do c.environment.add_env("DJANGO_DB_PASSWORD", values.wger.db_password) %}
  {% do c.environment.add_env("DJANGO_DB_HOST", values.consts.postgres_container_name) %}
  {% do c.environment.add_env("DJANGO_DB_PORT", postgres.get_port()) %}
  {% do c.environment.add_env("DJANGO_PERFORM_MIGRATIONS", "True") %}
  {% do c.environment.add_env("DJANGO_CACHE_BACKEND", "django_redis.cache.RedisCache") %}
  {% do c.environment.add_env("DJANGO_CACHE_LOCATION", "%s/1" | format(redis.get_url("redis"))) %}
  {% do c.environment.add_env("DJANGO_CACHE_CLIENT_PASSWORD", values.wger.redis_password) %}
  {% do c.environment.add_env("DJANGO_CACHE_TIMEOUT", 1296000) %}
  {% do c.environment.add_env("DJANGO_CACHE_CLIENT_CLASS", "django_redis.client.DefaultClient") %}
  {% do c.environment.add_env("DJANGO_COLLECTSTATIC_ON_STARTUP", "True") %}
  {% do c.environment.add_env("CSRF_TRUSTED_ORIGINS", origins.x|join(",")) %}
  {% if values.wger.powersync.enabled %}
    {% do c.environment.add_env("POWERSYNC_URL_PATH", values.wger.powersync.ps_url_path) %}
  {% endif %}
  {% do c.environment.add_user_envs(values.wger.additional_envs) %}
{% endfor %}

{# -- NGINX -- #}
{% do nginx.depends.add_dependency(values.consts.server_container_name, "service_healthy") %}
{% if values.wger.powersync.enabled %}
  {% do nginx.depends.add_dependency(values.consts.powersync_container_name, "service_healthy") %}
{% endif %}
{% do nginx.healthcheck.set_test("curl", {"port": values.network.web_port.port_number, "path": "/health"}) %}
{% do nginx.configs.add("nginx.conf", nginx_config(values), "/etc/nginx/conf.d/default.conf") %}
{% do nginx.add_storage("/wger/static", dict(values.storage.static, **{"read_only": True})) %}
{% do nginx.add_storage("/wger/media", dict(values.storage.media, **{"read_only": True})) %}
{% do nginx.add_port(values.network.web_port) %}

{# -- Server -- #}
{% do server.healthcheck.set_test("wget", {"port": values.consts.internal_server_port}) %}
{% do server.healthcheck.set_start_period(120) %}
{% do server.environment.add_env("WGER_PORT", values.consts.internal_server_port) %}
{% do server.depends.add_dependency(values.consts.postgres_container_name, "service_healthy") %}
{% do server.depends.add_dependency(values.consts.redis_container_name, "service_healthy") %}
{% do server.add_storage("/home/wger/static", values.storage.static) %}
{% do server.add_storage("/home/wger/media", values.storage.media) %}

{# -- Worker -- #}
{% do worker.healthcheck.set_custom_test(["CMD", "celery", "-A", "wger", "inspect", "ping"]) %}
{% do worker.set_command(["/start-worker"]) %}
{% do worker.depends.add_dependency(values.consts.server_container_name, "service_healthy") %}
{% do worker.add_storage("/home/wger/media", values.storage.media) %}

{# -- Beat -- #}
{% do beat.healthcheck.set_custom_test(["CMD", "celery", "-A", "wger", "inspect", "ping"]) %}
{% do beat.set_command(["/start-beat"]) %}
{% do beat.depends.add_dependency(values.consts.worker_container_name, "service_healthy") %}
{% do beat.add_storage("/home/wger/beat", values.storage.beat) %}

{# -- PowerSync (optional) -- #}
{% if values.wger.powersync.enabled %}
  {% set powersync = tpl.add_container(values.consts.powersync_container_name, "powersync_image") %}
  {% do powersync.add_network(wger_net) %}
  {% do powersync.set_command(["start", "-r", "unified"]) %}

  {# PowerSync config: literal values substituted at template time (no !env tags) #}
  {% set ps_db_uri = postgres.get_url("postgres") %}
  {% set ps_storage_uri = "postgres://powersync_storage:%s@%s:%s/%s?sslmode=disable" | format(
      values.wger.powersync.ps_storage_password,
      values.consts.postgres_container_name,
      postgres.get_port(),
      values.consts.db_name
  ) %}
  {% set ps_port = values.wger.powersync.ps_port %}

  {% set powersync_yaml %}
telemetry:
  disable_telemetry_sharing: true
  prometheus_port: 9090

replication:
  connections:
    - type: postgresql
      uri: {{ ps_db_uri }}
      sslmode: disable

storage:
  type: postgresql
  uri: {{ ps_storage_uri }}
  sslmode: disable

port: {{ ps_port }}

api:
  parameters:
    max_parameter_query_results: 2000

sync_rules:
  path: sync_rules.yaml

client_auth:
  allow_local_jwks: true
  jwks_uri: http://{{ values.consts.server_container_name }}:{{ values.consts.internal_server_port }}/api/v2/powersync-keys
  audience: ["powersync"]
  {% endset %}

  {% set sync_rules_yaml %}
config:
  edition: 3

streams:
  core:
    auto_subscribe: true
    queries:
      - SELECT * FROM core_language
      - SELECT * FROM core_license
      - SELECT * FROM core_repetitionunit
      - SELECT * FROM core_weightunit
      - SELECT * FROM exercises_exercise
      - SELECT * FROM exercises_translation
      - SELECT * FROM exercises_alias
      - SELECT * FROM exercises_exercisecomment
      - SELECT * FROM exercises_muscle
      - SELECT * FROM exercises_exercise_muscles
      - SELECT * FROM exercises_exercise_muscles_secondary
      - SELECT * FROM exercises_equipment
      - SELECT * FROM exercises_exercise_equipment
      - SELECT * FROM exercises_exercisecategory
      - SELECT * FROM exercises_exerciseimage
      - SELECT * FROM exercises_exercisevideo

  user_profile:
    auto_subscribe: true
    queries:
      - SELECT * FROM core_userprofile WHERE CAST(user_id AS TEXT) = auth.user_id()
      - SELECT * FROM gallery_image WHERE CAST(user_id AS TEXT) = auth.user_id()

  user_ingredients:
    auto_subscribe: true
    with:
      user_ingredients: |
        SELECT DISTINCT nutrition_synced_ingredient.id
        FROM nutrition_synced_ingredient
        WHERE nutrition_synced_ingredient.id IN (
                SELECT nutrition_logitem.ingredient_id FROM nutrition_logitem
                JOIN nutrition_nutritionplan ON nutrition_logitem.plan_id = nutrition_nutritionplan.id
                WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id())
           OR nutrition_synced_ingredient.id IN (
                SELECT nutrition_mealitem.ingredient_id FROM nutrition_mealitem
                JOIN nutrition_meal ON nutrition_mealitem.meal_id = nutrition_meal.id
                JOIN nutrition_nutritionplan ON nutrition_meal.plan_id = nutrition_nutritionplan.id
                WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id())
    queries:
      - SELECT * FROM nutrition_synced_ingredient AS nutrition_ingredient WHERE id IN user_ingredients
      - |
        SELECT nutrition_image.* FROM nutrition_image
        WHERE nutrition_image.ingredient_id IN user_ingredients
      - |
        SELECT nutrition_ingredientweightunit.* FROM nutrition_ingredientweightunit
        WHERE nutrition_ingredientweightunit.ingredient_id IN user_ingredients

  user_planning:
    auto_subscribe: true
    queries:
      - SELECT * FROM manager_routine WHERE CAST(user_id AS TEXT) = auth.user_id() AND is_template = FALSE
      - SELECT * FROM measurements_category WHERE CAST(user_id AS TEXT) = auth.user_id()
      - |
        SELECT measurements_measurement.*
        FROM measurements_measurement
        INNER JOIN measurements_category
          ON measurements_measurement.category_id = measurements_category.id
        WHERE CAST(measurements_category.user_id AS TEXT) = auth.user_id()
      - SELECT * FROM nutrition_nutritionplan WHERE CAST(user_id AS TEXT) = auth.user_id()
      - |
        SELECT nutrition_meal.*
        FROM nutrition_meal
        JOIN nutrition_nutritionplan
          ON nutrition_meal.plan_id = nutrition_nutritionplan.id
        WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id()
      - |
        SELECT nutrition_mealitem.*
        FROM nutrition_mealitem
        JOIN nutrition_meal
          ON nutrition_mealitem.meal_id = nutrition_meal.id
        JOIN nutrition_nutritionplan
          ON nutrition_meal.plan_id = nutrition_nutritionplan.id
        WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id()

  user_activity:
    auto_subscribe: true
    queries:
      - SELECT uuid AS id, weight, date, user_id FROM weight_weightentry WHERE CAST(user_id AS TEXT) = auth.user_id()
      - SELECT * FROM manager_workoutsession WHERE CAST(user_id AS TEXT) = auth.user_id()
      - SELECT * FROM manager_workoutlog WHERE CAST(user_id AS TEXT) = auth.user_id()
      - |
        SELECT nutrition_logitem.*
        FROM nutrition_logitem
        JOIN nutrition_nutritionplan
          ON nutrition_logitem.plan_id = nutrition_nutritionplan.id
        WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id()
  {% endset %}

  {% do powersync.configs.add("powersync.yaml", powersync_yaml, "/config/powersync.yaml") %}
  {% do powersync.configs.add("sync_rules.yaml", sync_rules_yaml, "/config/sync_rules.yaml") %}
  {% do powersync.environment.add_env("POWERSYNC_CONFIG_PATH", "/config/powersync.yaml") %}
  {% do powersync.healthcheck.set_test("wget", {"port": ps_port, "spider": false}) %}
  {% do powersync.depends.add_dependency(values.consts.postgres_container_name, "service_healthy") %}
  {% do powersync.depends.add_dependency(values.consts.server_container_name, "service_healthy") %}
{% endif %}

{% do perm_container.add_or_skip_action("static", values.storage.static, perms_config) %}
{% do perm_container.add_or_skip_action("media", values.storage.media, perms_config) %}
{% do perm_container.add_or_skip_action("beat", values.storage.beat, perms_config) %}
{% for store in values.storage.additional_storage %}
  {% do perm_container.add_or_skip_action(store.mount_path, store, perms_config) %}
{% endfor %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}

  {% set all_containers = containers + [nginx] %}
  {% for c in all_containers %}
    {% do c.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% endfor %}

  {% if values.wger.powersync.enabled %}
    {% do powersync.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% endif %}

  {% do postgres.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% do redis.container.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{% do tpl.portals.add(values.network.web_port) %}
{% do tpl.notes.set_body(values.consts.notes_body) %}

{{ tpl.render() | tojson }}
