# The production image map is inherited from ix_values.yaml. All configurable
# values below remain synthetic and disposable.

vaultbridge:
  api_key: vb081-development-placeholder-not-a-secret
  api_key_previous: ""
  watch_enabled: false
  watch_debounce_seconds: 1

run_as:
  user: 568
  group: 568

network:
  web_port:
    bind_mode: published
    port_number: 30488
    host_ips: []
  networks: []

storage:
  vault:
    type: host_path
    read_only: false
    host_path_config:
      acl_enable: false
      auto_permissions: false
      path: /opt/tests/mnt/vaultbridge/vault
  data:
    type: host_path
    host_path_config:
      acl_enable: false
      auto_permissions: false
      path: /opt/tests/mnt/vaultbridge/data-host-path

labels: []

resources:
  limits:
    cpus: 2
    memory: 4096
