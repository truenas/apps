# nexmail

[nexmail](https://github.com/DerKezorm/nexmail) is a self-hosted email client in the browser. It
brings several IMAP mailboxes together in one window, with a calendar over CalDAV and address books
over CardDAV, in a single container with one SQLite database.
