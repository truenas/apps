resources:
  limits:
    cpus: 2.0
    memory: 4096

komodo:
  passkey: secret
  jwt_secret: secret
  db_password: secret
  additional_envs: []

network:
  web_port:
    bind_mode: published
    port_number: 8080

ix_volumes:
  mongo_data: /opt/tests/mnt/komodo/mongo_data
  repo_cache: /opt/tests/mnt/komodo/repo_cache
  syncs: /opt/tests/mnt/komodo/syncs
  periphery_root: /opt/tests/mnt/komodo/periphery_root

storage:
  mongodb_data:
    type: ix_volume
    ix_volume_config:
      dataset_name: mongo_data
      create_host_path: true
  repo_cache:
    type: ix_volume
    ix_volume_config:
      dataset_name: repo_cache
      create_host_path: true
  syncs:
    type: ix_volume
    ix_volume_config:
      dataset_name: syncs
      create_host_path: true
  periphery_root:
    type: ix_volume
    ix_volume_config:
      dataset_name: periphery_root
      create_host_path: true
  additional_storage: []
