groups:
  - name: CineFind Bridge Configuration
    description: Connect CineFind to local Seerr, Radarr and Sonarr services without exposing them online. Configure at least one service.
  - name: Storage Configuration
    description: Store the revocable device token locally on TrueNAS.
  - name: Labels Configuration
    description: Configure additional container labels.
  - name: Resources Configuration
    description: Configure CPU and memory limits.

questions:
  - variable: bridge
    label: ''
    group: CineFind Bridge Configuration
    schema:
      type: dict
      attrs:
        - variable: pairing_code
          label: CineFind Pairing Code
          description: Create this one-time 10-character code in CineFind Account → CineFind Bridge. It expires after 15 minutes.
          schema:
            type: string
            default: ''
            required: true
            private: true
        - variable: seerr_url
          label: Local Seerr URL (Optional)
          description: Your local Seerr URL, for example http://192.168.178.195:5055. Leave both Seerr fields empty if unused.
          schema:
            type: string
            default: ''
            required: false
        - variable: seerr_api_key
          label: Seerr API Key (Optional)
          description: Create this key in Seerr. It stays inside this TrueNAS app and is never sent to CineFind.
          schema:
            type: string
            default: ''
            required: false
            private: true
        - variable: radarr_url
          label: Local Radarr URL (Optional)
          description: Your local Radarr URL, for example http://192.168.178.195:7878. Used for movie requests.
          schema:
            type: string
            default: ''
            required: false
        - variable: radarr_api_key
          label: Radarr API Key (Optional)
          description: Found under Radarr Settings → General → Security. It stays local and is never sent to CineFind.
          schema:
            type: string
            default: ''
            required: false
            private: true
        - variable: sonarr_url
          label: Local Sonarr URL (Optional)
          description: Your local Sonarr URL, for example http://192.168.178.195:8989. Used for series requests.
          schema:
            type: string
            default: ''
            required: false
        - variable: sonarr_api_key
          label: Sonarr API Key (Optional)
          description: Found under Sonarr Settings → General → Security. It stays local and is never sent to CineFind.
          schema:
            type: string
            default: ''
            required: false
            private: true
        - variable: display_name
          label: Device Name
          description: A friendly name shown in your CineFind account.
          schema:
            type: string
            default: TrueNAS
            max_length: 80
            required: true
  - variable: storage
    label: ''
    group: Storage Configuration
    schema:
      type: dict
      attrs:
        - variable: data
          label: Bridge Data Storage
          description: Stores a revocable device token. Do not delete it unless you intend to pair again.
          schema:
            type: dict
            attrs:
              - variable: type
                label: Storage Type
                schema:
                  type: string
                  required: true
                  default: ix_volume
                  enum:
                    - value: ix_volume
                      description: ixVolume (managed by TrueNAS)
                    - value: host_path
                      description: Host Path (an existing TrueNAS directory)
              - variable: ix_volume_config
                label: ixVolume Configuration
                schema:
                  type: dict
                  show_if: [[type, '=', ix_volume]]
                  $ref:
                    - normalize/ix_volume
                  attrs:
                    - variable: acl_enable
                      label: Enable ACL
                      schema:
                        type: boolean
                        default: false
                    - variable: dataset_name
                      label: Dataset Name
                      schema:
                        type: string
                        hidden: true
                        default: data
                        required: true
                    - variable: acl_entries
                      label: ACL Configuration
                      schema:
                        type: dict
                        show_if: [[acl_enable, '=', true]]
                        $ref:
                          - normalize/acl
                        attrs: []
              - variable: host_path_config
                label: Host Path Configuration
                schema:
                  type: dict
                  show_if: [[type, '=', host_path]]
                  attrs:
                    - variable: acl_enable
                      label: Enable ACL
                      schema:
                        type: boolean
                        default: false
                    - variable: acl
                      label: ACL Configuration
                      schema:
                        type: dict
                        show_if: [[acl_enable, '=', true]]
                        $ref:
                          - normalize/acl
                        attrs: []
                    - variable: path
                      label: Host Path
                      schema:
                        type: hostpath
                        required: true
  - variable: labels
    label: ''
    group: Labels Configuration
    schema:
      type: list
      default: []
      items:
        - variable: label
          label: Label
          schema:
            type: dict
            attrs:
              - variable: key
                label: Key
                schema:
                  type: string
                  required: true
              - variable: value
                label: Value
                schema:
                  type: string
                  required: true
              - variable: containers
                label: Containers
                description: Containers where the label should be applied.
                schema:
                  type: list
                  items:
                    - variable: container
                      label: Container
                      schema:
                        type: string
                        required: true
                        enum:
                          - value: cinefind-bridge
                            description: CineFind Bridge
  - variable: resources
    label: ''
    group: Resources Configuration
    schema:
      type: dict
      attrs:
        - variable: limits
          label: Limits
          schema:
            type: dict
            attrs:
              - variable: cpus
                label: CPUs
                description: CPU limit for CineFind Bridge.
                schema:
                  type: int
                  default: 1
                  required: true
              - variable: memory
                label: Memory (in MB)
                description: Memory limit for CineFind Bridge.
                schema:
                  type: int
                  default: 256
                  required: true
