{% set tpl = ix_lib.base.render.Render(values) %}

{% set c1 = tpl.add_container(values.consts.trawl_container_name, "image") %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}

{% do c1.set_user(0, 0) %}
{# shm scales with the browser pool: single instance fits 512MB, >5 needs 2GB. #}
{% set shm_mb = 512 if values.trawl.browser_pool_size == 1 else 2048 if values.trawl.browser_pool_size > 5 else 1024 %}
{% do c1.set_shm_size_mb(shm_mb) %}

{% do c1.healthcheck.set_test("curl", {"port": values.network.web_port.port_number, "path": "/health"}) %}
{% do c1.healthcheck.set_start_period(90) %}

{% do c1.add_port(values.network.web_port) %}

{% do c1.environment.add_env("PORT", values.network.web_port.port_number) %}
{% do c1.environment.add_env("BROWSER_POOL_SIZE", values.trawl.browser_pool_size) %}
{% do c1.environment.add_env("PROXY_URL", values.trawl.proxy_url) %}
{% do c1.environment.add_env("RESIDENTIAL_PROXY_URL", values.trawl.residential_proxy_url) %}

{% set redis_config = {
  "password": values.consts.redis_password,
  "volume": {"type": "temporary", "volume_config": {"volume_name": "redis-data"}},
} %}
{% set redis = tpl.deps.redis(values.consts.redis_container_name, "redis_image", redis_config, perm_container) %}
{% do c1.depends.add_dependency(values.consts.redis_container_name, "service_healthy") %}
{% do c1.environment.add_env("REDIS_URL", redis.get_url("redis")) %}

{% do c1.environment.add_user_envs(values.trawl.additional_envs) %}

{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
{% endfor %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% do redis.container.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{{ tpl.render() | tojson }}
