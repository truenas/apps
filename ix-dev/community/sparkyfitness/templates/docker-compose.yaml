{% set tpl = ix_lib.base.render.Render(values) %}

{% set sparky_net = tpl.networks.create_internal("sparky-net") %}

{% set frontend = tpl.add_container(values.consts.frontend_container_name, "image") %}
{% set server = tpl.add_container(values.consts.server_container_name, "server_image") %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perms_config = {"uid": values.run_as.user, "gid": values.run_as.group, "mode": "check"} %}

{% set pg_config = {
  "user": values.consts.db_user,
  "password": values.sparkyfitness.db_password,
  "database": values.consts.db_name,
  "volume": values.storage.postgres_data,
} %}
{% set postgres = tpl.deps.postgres(values.consts.postgres_container_name, values.sparkyfitness.postgres_image_selector, pg_config, perm_container) %}
{% do postgres.container.add_network(sparky_net) %}

{% do frontend.add_network(sparky_net) %}
{% do frontend.set_user(values.run_as.user, values.run_as.group) %}
{% do frontend.healthcheck.set_test("curl", {"port": values.network.web_port.port_number, "path": "/"}) %}
{% do frontend.depends.add_dependency(values.consts.server_container_name, "service_healthy") %}

{% do frontend.environment.add_env("SPARKY_FITNESS_SERVER_HOST", values.consts.server_container_name) %}
{% do frontend.environment.add_env("SPARKY_FITNESS_SERVER_PORT", values.consts.server_port) %}
{% do frontend.environment.add_env("SPARKY_FITNESS_FRONTEND_URL", values.sparkyfitness.frontend_url.removesuffix("/")) %}
{% do frontend.environment.add_env("NGINX_LISTEN_PORT", values.network.web_port.port_number) %}

{% do frontend.add_port(values.network.web_port) %}

{% set tmpfs_config = {"type": "tmpfs", "tmpfs_config": {"uid": values.run_as.user, "gid": values.run_as.user, "size": 10}} %}
{% do frontend.add_storage("/etc/nginx/conf.d", tmpfs_config) %}
{% do frontend.add_storage("/run", tmpfs_config) %}
{% do frontend.add_storage("/var/cache/nginx", tmpfs_config) %}

{% do server.add_network(sparky_net) %}
{% do server.set_user(values.run_as.user, values.run_as.group) %}
{% do server.healthcheck.set_test("curl", {"port": values.consts.server_port, "path": "/api/health"}) %}
{% do server.depends.add_dependency(values.consts.postgres_container_name, "service_healthy") %}

{% do server.environment.add_env("NODE_ENV", "production") %}
{% do server.environment.add_env("SPARKY_FITNESS_DB_HOST", values.consts.postgres_container_name) %}
{% do server.environment.add_env("SPARKY_FITNESS_DB_PORT", postgres.get_port()) %}
{% do server.environment.add_env("SPARKY_FITNESS_DB_NAME", values.consts.db_name) %}
{% do server.environment.add_env("SPARKY_FITNESS_DB_USER", values.consts.db_user) %}
{% do server.environment.add_env("SPARKY_FITNESS_DB_PASSWORD", values.sparkyfitness.db_password) %}
{% do server.environment.add_env("SPARKY_FITNESS_APP_DB_USER", values.consts.db_user) %}
{% do server.environment.add_env("SPARKY_FITNESS_APP_DB_PASSWORD", values.sparkyfitness.db_password) %}
{% do server.environment.add_env("SPARKY_FITNESS_API_ENCRYPTION_KEY", values.sparkyfitness.api_encryption_key) %}
{% do server.environment.add_env("BETTER_AUTH_SECRET", values.sparkyfitness.better_auth_secret) %}
{% do server.environment.add_env("SPARKY_FITNESS_FRONTEND_URL", values.sparkyfitness.frontend_url.removesuffix("/")) %}
{% do server.environment.add_env("SPARKY_FITNESS_SERVER_PORT", values.consts.server_port) %}
{% do server.environment.add_env("SPARKY_FITNESS_CUSTOM_TEMP_DIRECTORY", "/app/SparkyFitnessServer/backup/temp_uploads") %}

{% do server.environment.add_user_envs(values.sparkyfitness.additional_envs) %}

{% do server.add_storage("/app/SparkyFitnessServer/backup", values.storage.backup) %}
{% do server.add_storage("/app/SparkyFitnessServer/uploads", values.storage.uploads) %}

{% do perm_container.add_or_skip_action("backup", values.storage.backup, perms_config) %}
{% do perm_container.add_or_skip_action("uploads", values.storage.uploads, perms_config) %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do postgres.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% do server.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% do frontend.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{% do tpl.portals.add(values.network.web_port) %}

{{ tpl.render() | tojson }}
