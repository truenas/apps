resources:
  limits:
    cpus: 1.0
    memory: 256

monitee_agent:
  docker_enable: true
  storage_monitoring_enable: true
  systemd_journald_enable: true

network:
  https_port: 8443
  http_port: 8080
  host_network: true

ix_volumes:
  config: /opt/tests/mnt/config
  data: /opt/tests/mnt/data

storage:
  data:
    type: ix_volume
    ix_volume_config:
      dataset_name: data
      create_host_path: true
  config:
    type: ix_volume
    ix_volume_config:
      dataset_name: config
      create_host_path: true
