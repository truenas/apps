# zfs-autobackup

[zfs-autobackup](https://github.com/psy0rz/zfs_autobackup) creates ZFS snapshots on a schedule, replicates them
to another pool or to a remote machine over SSH, and thins out old snapshots according to a retention schedule.
