{% set tpl = ix_lib.base.render.Render(values) %}

{% set c1 = tpl.add_container(values.consts.hermes_agent_container_name, "image") %}
{% do c1.set_user(0, 0) %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perms_config = {"uid": 0, "gid": 0, "mode": "check"} %}

{% if values.hermes_agent.enable_dashboard == true %}
  {% do c1.healthcheck.set_test("curl", {"port": values.network.dashboard_port.port_number, "path": "/health"}) %}
{% elif values.hermes_agent.enable_gateway == true %}
  {% do c1.healthcheck.set_test("curl", {"port": values.network.gateway_port.port_number, "path": "/health"}) %}
{% else %}
  {% do c1.healthcheck.disable() %}
{% endif %}

{% do c1.set_command([
  "gateway", "run"
]) %}


{% if values.hermes_agent.enable_dashboard == true %}
  {% do c1.environment.add_env("HERMES_DASHBOARD", "1") %}
  {% do c1.environment.add_env("HERMES_DASHBOARD_PORT", values.network.dashboard_port.port_number) %}
  
  {% if values.hermes_agent.enable_insecure_dashboard_access == true %}
    {% do c1.environment.add_env("HERMES_DASHBOARD_INSECURE", "1") %}
  {% endif %}

{% endif %}

{% if values.hermes_agent.enable_gateway == true %}
  {% do c1.environment.add_env("API_SERVER_ENABLED", values.hermes_agent.enable_gateway) %}
  {% do c1.environment.add_env("API_SERVER_PORT", values.network.gateway_port.port_number) %}
  {% do c1.environment.add_env("API_SERVER_KEY", values.hermes_agent.gateway_key) %}
  {% do c1.environment.add_env("GATEWAY_ALLOW_ALL_USERS", values.hermes_agent.allow_all_users_access) %}
{% endif %}

{% do c1.environment.add_user_envs(values.hermes_agent.additional_envs) %}


{% if values.hermes_agent.enable_gateway == true %}
  {% do c1.add_port(values.network.gateway_port) %}
{% endif %}

{% if values.hermes_agent.enable_dashboard == true %}
  {% do c1.add_port(values.network.dashboard_port) %}
{% endif %}


{% do c1.add_storage(values.consts.data_dir, values.storage.data) %}
{% do perm_container.add_or_skip_action("data", values.storage.data, perms_config) %}

{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
  {% do perm_container.add_or_skip_action(store.mount_path, store, perms_config) %}
{% endfor %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}


{% if values.hermes_agent.enable_dashboard == true %}
  {% do tpl.portals.add(values.network.dashboard_port) %}
{% endif %}


{% do c1.add_caps(["CHOWN", "SETUID", "SETGID", "DAC_OVERRIDE"]) %}

{{ tpl.render() | tojson }}
