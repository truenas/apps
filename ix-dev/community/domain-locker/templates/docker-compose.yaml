{% set tpl = ix_lib.base.render.Render(values) %}

{% set dl_net = tpl.networks.create_internal("dl-net") %}

{% set c1 = tpl.add_container(values.consts.app_container_name, "image") %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}

{# Postgres #}
{% set pg_config = {
  "user": values.consts.db_user,
  "password": values.domain_locker.db_password,
  "database": values.consts.db_name,
  "volume": values.storage.postgres_data,
} %}
{% set postgres = tpl.deps.postgres(
  values.consts.postgres_container_name,
  values.domain_locker.postgres_image_selector,
  pg_config, perm_container
) %}
{% do postgres.container.add_network(dl_net) %}

{# Main app container #}
{% do c1.add_network(dl_net) %}
{% do c1.set_user(values.run_as.user, values.run_as.group) %}
{% do c1.depends.add_dependency(values.consts.postgres_container_name, "service_healthy") %}
{% do c1.healthcheck.set_test("wget", {"port": values.network.web_port.port_number, "path": "/api/health"}) %}

{% do c1.environment.add_env("PORT", values.network.web_port.port_number) %}
{% do c1.environment.add_env("DL_ENV_TYPE", "selfHosted") %}
{% do c1.environment.add_env("DL_PG_HOST", values.consts.postgres_container_name) %}
{% do c1.environment.add_env("DL_PG_PORT", 5432) %}
{% do c1.environment.add_env("DL_PG_NAME", values.consts.db_name) %}
{% do c1.environment.add_env("DL_PG_USER", values.consts.db_user) %}
{% do c1.environment.add_env("DL_PG_PASSWORD", values.domain_locker.db_password) %}
{% do c1.environment.add_user_envs(values.domain_locker.additional_envs) %}

{% do c1.add_port(values.network.web_port) %}

{# Updater cron sidecar — runs as root because crond needs to write crontabs #}
{% set c_updater = tpl.add_container(values.consts.updater_container_name, "alpine_image") %}
{% do c_updater.add_network(dl_net) %}
{% do c_updater.set_user(0, 0) %}
{% do c_updater.deploy.resources.set_profile("low") %}
{% do c_updater.add_caps(["SETUID", "SETGID"]) %}
{% do c_updater.depends.add_dependency(values.consts.app_container_name, "service_healthy") %}
{% do c_updater.healthcheck.set_test("pidof", {"process": "crond"}) %}
{% do c_updater.build_image([
  "RUN apk add --no-cache curl || { echo 'Failed to install curl'; exit 1; }",
  "RUN (echo '*/15 * * * * /usr/bin/curl -f -X POST http://%s:%d/api/domain-monitor' >> /etc/crontabs/root && echo '0 3 * * * /usr/bin/curl -f -X POST http://%s:%d/api/domain-updater' >> /etc/crontabs/root && echo '0 4 * * * /usr/bin/curl -f -X POST http://%s:%d/api/expiration-reminders' >> /etc/crontabs/root && echo '0 2 * * 0 /usr/bin/curl -f -X POST http://%s:%d/api/cleanup-monitor-data' >> /etc/crontabs/root) || { echo 'Failed to set cron jobs'; exit 1; }"|format(
    values.consts.app_container_name, values.network.web_port.port_number,
    values.consts.app_container_name, values.network.web_port.port_number,
    values.consts.app_container_name, values.network.web_port.port_number,
    values.consts.app_container_name, values.network.web_port.port_number
  ),
  'ENTRYPOINT ["crond", "-f", "-l", "2"]'
]) %}

{# Storage — no app-specific volumes needed, only postgres data #}
{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
{% endfor %}

{# Permissions container activation #}
{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% do postgres.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{% do tpl.portals.add(values.network.web_port) %}

{{ tpl.render() | tojson }}
