resources:
  limits:
    cpus: 2.0
    memory: 4096

cloudflare_mesh:
  mesh_token: ""
  additional_args: []
  additional_envs: []

network:
  host_network: false

ix_volumes:
  config: /opt/tests/mnt/config

storage:
  config:
    type: ix_volume
    ix_volume_config:
      dataset_name: config
      create_host_path: true
  additional_storage: []
