# GitLab milestone versions kept in the catalog for safe step-by-step upgrades.
# See https://docs.gitlab.com/update/upgrade_paths/ — skipping milestones corrupts data.
# Remove an entry when that major line is EOL and no user is expected to still run it.
# When a new major (e.g. 20.x) supersedes the current one, add the last patch of the
# superseded major here so users who fell behind can step up.

# Last version of the v18.x.x line (previous major before the 19.x debut).
- 18.11.7