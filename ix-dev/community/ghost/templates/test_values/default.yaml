resources:
  limits:
    cpus: 2.0
    memory: 4096

ghost:
  url: http://localhost:2368
  db_password: db-ghost-password
  db_root_password: root-db-ghost-password
  additional_envs: []

network:
  web_port:
    bind_mode: published
    port_number: 2368

ix_volumes:
  content: /opt/tests/mnt/content
  mysql_data: /opt/tests/mnt/mysql_data

storage:
  content:
    type: ix_volume
    ix_volume_config:
      dataset_name: content
      create_host_path: true
  mysql_data:
    type: ix_volume
    ix_volume_config:
      dataset_name: mysql_data
      create_host_path: true
  additional_storage: []
