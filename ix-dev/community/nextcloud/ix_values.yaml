images:
  # TODO: Switch back to a pinned tag once a build containing the talk
  # configure script is published.
  image:
    repository: ixsystems/nextcloud-fpm
    tag: unstable
  container_utils_image:
    repository: ixsystems/container-utils
    tag: 1.0.2
  notify_push_image:
    repository: ixsystems/nextcloud-notify-push
    tag: 1.4.0-8423ff86
  nginx_image:
    repository: nginxinc/nginx-unprivileged
    tag: 1.31.5
  postgres_17_image:
    repository: postgres
    tag: 17.11-bookworm
  postgres_18_image:
    repository: postgres
    tag: 18.6-trixie
  redis_image:
    repository: valkey/valkey
    tag: 9.1.2
  imaginary_image:
    repository: ghcr.io/nextcloud-releases/aio-imaginary
    tag: "20260825_084538"
  # TODO: Switch to ghcr.io/nextcloud-releases/aio-talk once a tag containing
  # the dinit migration is published. It is currently only in upstream main,
  # and the published tags still run supervisord.
  talk_image:
    repository: truenas/aio-talk
    tag: dinit-local
  harp_image:
    repository: ghcr.io/nextcloud/nextcloud-appapi-harp
    tag: v0.4.5
  postgres_upgrade_image:
    repository: ixsystems/postgres-upgrade
    tag: 1.2.16

consts:
  nextcloud_container_name: nextcloud
  cron_container_name: cron
  perms_container_name: permissions
  redis_container_name: redis
  postgres_container_name: postgres
  nginx_container_name: nginx
  imaginary_container_name: imaginary
  notify_push_container_name: notify-push
  harp_container_name: harp
  talk_container_name: talk
  db_user: nextcloud
  db_name: nextcloud
  internal_nextcloud_port: 9000
  internal_imaginary_port: 9000
  internal_notify_push_port: 7867
  internal_harp_port: 8780
  internal_harp_frp_port: 8782
  internal_talk_signaling_port: 8081

  html_path: /var/www/html
  data_path: /nc-data

  ssl_key_path: /etc/nginx-certs/private.key
  ssl_cert_path: /etc/nginx-certs/public.crt

  notes: |
    To use `occ` commands, you just shell into the
    nextcloud container and run `occ` directly.
    No need to use neither `sudo` or `php`.

    For example `occ status`.
