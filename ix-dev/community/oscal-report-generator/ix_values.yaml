images:
  image:
    repository: keekar/oscal_reports
    tag: latest

consts:
  app_container_name: oscal-report-generator
  perms_container_name: oscal-perms

# Run as context - matches app.yaml
run_as:
  user: 0
  group: 0

# Application defaults
app:
  node_env: production
  port: 3020
