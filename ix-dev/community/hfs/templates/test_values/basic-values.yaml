admin_password: test-password
web_port: 30492
# the config volume creates this disposable parent directory before HFS starts
shared_folder: /opt/tests/mnt
advanced: false
basic_config_storage:
  type: ix_volume
  ix_volume_config:
    dataset_name: config
    create_host_path: true
ix_volumes:
  config: /opt/tests/mnt/config
