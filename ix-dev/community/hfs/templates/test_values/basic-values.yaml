admin_password: test-password
web_port: 30492
# basic mode needs an existing share; use public system data without creating host directories
shared_folder: /usr/share/zoneinfo
advanced: false
basic_config_storage:
  type: ix_volume
  ix_volume_config:
    dataset_name: config
    create_host_path: true
ix_volumes:
  config: /opt/tests/mnt/config
