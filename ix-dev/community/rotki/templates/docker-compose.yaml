{% set tpl = ix_lib.base.render.Render(values) %}

{% set c1 = tpl.add_container(values.consts.rotki_container_name, "image") %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perms_config = {"uid": values.run_as.user, "gid": values.run_as.group, "mode": "check"} %}

{% do c1.set_user(values.run_as.user, values.run_as.group) %}
{% do c1.set_read_only(true) %}
{% do c1.set_grace_period(30) %}
{% do c1.healthcheck.set_custom_test(["CMD", "/opt/rotki/starling", "healthcheck"]) %}
{% do c1.healthcheck.set_start_period(120) %}

{% do c1.environment.add_env("ROTKI_HTTP_PORT", values.network.web_port.port_number) %}
{% do c1.environment.add_env("ROTKI_SESSION_KEY", values.rotki.session_key) %}
{% do c1.environment.add_env("ROTKI_SESSION_COOKIE_SECURE", values.rotki.secure_cookie) %}
{% do c1.environment.add_user_envs(values.rotki.additional_envs) %}
{% do c1.add_port(values.network.web_port) %}

{# The distroless supervisor and frozen Python backend need writable scratch space. #}
{% do c1.add_storage("/tmp", {"type": "tmpfs", "tmpfs_config": {"size": 512, "uid": values.run_as.user, "gid": values.run_as.group}}) %}
{% do c1.add_storage("/run", {"type": "tmpfs", "tmpfs_config": {"size": 16, "uid": values.run_as.user, "gid": values.run_as.group}}) %}
{% do c1.add_storage("/config", {"type": "tmpfs", "tmpfs_config": {"size": 1, "uid": values.run_as.user, "gid": values.run_as.group}}) %}

{% do c1.add_storage(values.consts.data_path, values.storage.data) %}
{% do perm_container.add_or_skip_action("data", values.storage.data, perms_config) %}

{% do c1.add_storage(values.consts.logs_path, values.storage.logs) %}
{% do perm_container.add_or_skip_action("logs", values.storage.logs, perms_config) %}

{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
  {% do perm_container.add_or_skip_action(store.mount_path, store, perms_config) %}
{% endfor %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{% do tpl.portals.add(values.network.web_port) %}
{% do tpl.notes.add_info("Create your Rotki account in the Web UI. The Session Key secures browser login sessions. Changing it requires users to log in again. It does not encrypt your database.") %}
{% do tpl.notes.add_info("Keep this app on a trusted private network or behind an authenticating HTTPS reverse proxy. Select HTTPS cookies only when accessing the app through HTTPS.") %}

{{ tpl.render() | tojson }}
