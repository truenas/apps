{% set tpl = ix_lib.base.render.Render(values) %}

{% set c1 = tpl.add_container(values.consts.twingate_container_name, "image") %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perm_config = {"uid": values.run_as.user, "gid": values.run_as.group, "mode": "check"} %}

{% do c1.set_user(values.run_as.user, values.run_as.group) %}
{% do c1.healthcheck.use_built_in() %}
{% do c1.environment.add_env("TWINGATE_LABEL_HOSTNAME", values.twingate.hostname) %}
{% do c1.environment.add_env("TWINGATE_NETWORK", values.twingate.network_name) %}
{% do c1.environment.add_env("TWINGATE_ACCESS_TOKEN", values.twingate.access_token) %}
{% do c1.environment.add_env("TWINGATE_REFRESH_TOKEN", values.twingate.refresh_token) %}

{% do c1.environment.add_user_envs(values.twingate.additional_envs) %}

{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
  {% do perm_container.add_or_skip_action(store.mount_path, store, perm_config) %}
{% endfor %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{{ tpl.render() | tojson }}
