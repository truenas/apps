TZ: Etc/UTC

mkvtoolnix:
  additional_envs: []

network:
  web_port:
    bind_mode: published
    port_number: 8081
  host_network: false

run_as:
  user: 568
  group: 568

ix_volumes:
  config: /opt/tests/mnt/mkvtoolnix/config
  storage: /opt/tests/mnt/mkvtoolnix/storage

storage:
  config:
    type: ix_volume
    ix_volume_config:
      dataset_name: storage
      create_host_path: true
  storage:
    type: ix_volume
    ix_volume_config:
      dataset_name: storage
      create_host_path: true
  additional_storage: []
