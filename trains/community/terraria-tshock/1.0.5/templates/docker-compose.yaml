{% set tpl = ix_lib.base.render.Render(values) %}

{% set c1 = tpl.add_container(values.consts.terraria_container_name, "image") %}
{% do c1.set_tty(true) %}
{% do c1.set_stdin(true) %}

{% if values.ci %}
  {% do c1.healthcheck.set_test("tcp", {"port": values.network.server_port.port_number}) %}
{% else %}
  {% do c1.healthcheck.disable() %}
{% endif %}

{% do c1.environment.add_user_envs(values.terraria.additional_envs) %}

{#
  'worldevil', 'dificulty' and 'autocreate' flags are only used
  when a world is generated. According to docs server will
  ignore them if a world exists, so we can safely pass
  them all the time and let application handle it.

  We set -world (and not -worldname), as this is the only way to "autostart" it.
  Otherwise it waits to interactively select a world from the list of worlds (via the stdin).
#}

{% set cmd = namespace(x=[
  "-world", "/worlds/%s.wld" | format(values.terraria.world_file_name.removesuffix(".wld")),
  "-autocreate", values.terraria.world_size,
  "-difficulty", values.terraria.world_difficulty,
  "-worldevil", values.terraria.world_evil,
  "-maxplayers", values.terraria.max_players,
  "-port", values.network.server_port.port_number,
]) %}

{% if values.terraria.world_seed %}
  {% do cmd.x.extend(["-seed", values.terraria.world_seed]) %}
{% endif %}

{% if values.terraria.secure %}
  {% do cmd.x.append("-secure") %}
{% endif %}

{% if values.terraria.force_update %}
  {% do cmd.x.append("-forceupdate") %}
{% endif %}

{% if values.terraria.password %}
  {% do cmd.x.extend(["-password", values.terraria.password]) %}
{% endif %}

{% if values.network.api_port.bind_mode %}
  {% do cmd.x.extend([
    "-rest-enabled", true,
    "-rest-port", values.network.api_port.port_number,
  ]) %}
{% endif %}

{% for item in values.terraria.additional_args %}
  {% set flag = item.split("=")[0] %}
  {% if flag in values.consts.reserved_args %}
    {% do tpl.funcs.fail("Argument [%s] is automatically handled by the app, please use the corresponding field." | format(flag)) %}
  {% endif %}

  {% do cmd.x.append(flag) %}

  {% if item.value %}
    {% do cmd.x.append(item.value) %}
  {% endif %}
{% endfor %}

{% do c1.set_command(cmd.x) %}

{% do c1.add_port(values.network.server_port) %}
{% do c1.add_port(values.network.api_port) %}

{% do c1.add_storage("/worlds", values.storage.world) %}
{% do c1.add_storage("/tshock", values.storage.config) %}
{% do c1.add_storage("/plugins", values.storage.plugins) %}

{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
{% endfor %}

{{ tpl.render() | tojson }}
