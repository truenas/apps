{% set tpl = ix_lib.base.render.Render(values) %}

{% set cloudreve_net = tpl.networks.create_internal("cloudreve-net") %}

{% set c1 = tpl.add_container(values.consts.cloudreve_container_name, "image") %}
{% do c1.add_network(cloudreve_net) %}

{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perms_config = {"uid": values.run_as.user, "gid": values.run_as.group, "mode": "check"} %}

{% if values.cloudreve.enable_fts %}
  {% set meili_config = {"master_key": values.cloudreve.meilisearch_master_key, "volume": values.storage.meili_data} %}
  {% set meilisearch = tpl.deps.meilisearch(values.consts.meilisearch_container_name, "meilisearch_image", meili_config, perm_container) %}
  {% do meilisearch.container.add_network(cloudreve_net) %}

  {% set tika = tpl.deps.tika(values.consts.tika_container_name, "tika_image", {}) %}
  {% do tika.container.add_network(cloudreve_net) %}

  {% do c1.environment.add_env("CR_SETTING_DEFAULT_fts_enabled", 1) %}
  {% do c1.environment.add_env("CR_SETTING_DEFAULT_fts_tika_endpoint", tika.get_url()) %}
  {% do c1.environment.add_env("CR_SETTING_DEFAULT_fts_meilisearch_api_key", values.cloudreve.meilisearch_master_key) %}
  {% do c1.environment.add_env("CR_SETTING_DEFAULT_fts_meilisearch_endpoint", meilisearch.get_url()) %}

  {% do c1.depends.add_dependency(values.consts.meilisearch_container_name, "service_healthy") %}
  {% do c1.depends.add_dependency(values.consts.tika_container_name, "service_healthy") %}
{% endif %}

{% set pg_config = {
  "user": values.consts.db_user,
  "password": values.cloudreve.db_password,
  "database": values.consts.db_name,
  "volume": values.storage.postgres_data,
} %}
{% set postgres = tpl.deps.postgres(values.consts.postgres_container_name, values.cloudreve.postgres_image_selector, pg_config, perm_container) %}
{% do postgres.container.add_network(cloudreve_net) %}

{% set redis_config = {
  "password": values.cloudreve.redis_password,
  "volume": {"type": "temporary", "volume_config": {"volume_name": "redis-data"}},
} %}
{% set redis = tpl.deps.redis(values.consts.redis_container_name, "redis_image", redis_config, perm_container) %}
{% do redis.container.add_network(cloudreve_net) %}

{% do c1.set_user(values.run_as.user, values.run_as.group) %}
{% do c1.healthcheck.set_test("wget", {"port": values.network.web_port.port_number, "path": "/api/v4/site/ping"}) %}

{% do c1.depends.add_dependency(values.consts.postgres_container_name, "service_healthy") %}
{% do c1.depends.add_dependency(values.consts.redis_container_name, "service_healthy") %}

{% do c1.environment.add_env("CR_CONF_System.Listen", ":%d"|format(values.network.web_port.port_number)) %}
{% do c1.environment.add_env("CR_CONF_Database.Type", "postgres") %}
{% do c1.environment.add_env("CR_CONF_Database.DatabaseURL", postgres.get_url("postgres")) %}
{% do c1.environment.add_env("CR_CONF_Redis.Network", "tcp") %}
{% do c1.environment.add_env("CR_CONF_Redis.Server", "%s:%d"|format(values.consts.redis_container_name, redis.get_port())) %}
{% do c1.environment.add_env("CR_CONF_Redis.Password", values.cloudreve.redis_password) %}
{% do c1.environment.add_env("CR_CONF_Redis.DB", 0) %}
{% do c1.environment.add_user_envs(values.cloudreve.additional_envs) %}

{% do c1.add_port(values.network.web_port) %}

{% do c1.add_storage("/cloudreve/data", values.storage.data) %}
{% do perm_container.add_or_skip_action("data", values.storage.data, perms_config) %}

{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
  {% do perm_container.add_or_skip_action(store.mount_path, store, perms_config) %}
{% endfor %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% do postgres.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% do redis.container.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% if values.cloudreve.enable_fts %}
    {% do meilisearch.container.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
    {% do tika.container.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% endif %}
{% endif %}

{% do tpl.portals.add(values.network.web_port) %}

{{ tpl.render() | tojson }}
