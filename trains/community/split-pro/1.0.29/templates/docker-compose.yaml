{% set tpl = ix_lib.base.render.Render(values) %}

{% set split_net = tpl.networks.create_internal("split-pro-net") %}

{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perms_config = {"uid": values.run_as.user, "gid": values.run_as.group, "mode": "check"} %}

{% set pg_config = {
  "user": values.consts.db_user,
  "password": values.splitpro.db_password,
  "database": values.consts.db_name,
  "volume": values.storage.postgres_data,
  "additional_options": {
    "shared_preload_libraries": "pg_cron",
    "cron.database_name": values.consts.db_name,
    "cron.timezone": values.TZ,
  }
} %}
{% set postgres = tpl.deps.postgres(values.consts.postgres_container_name, values.splitpro.postgres_image_selector, pg_config, perm_container) %}
{% do postgres.container.add_network(split_net) %}

{% set c1 = tpl.add_container(values.consts.splitpro_container_name, "image") %}
{% do c1.add_network(split_net) %}
{% do c1.set_user(values.run_as.user, values.run_as.group) %}
{% do c1.depends.add_dependency(values.consts.postgres_container_name, "service_healthy") %}
{% do c1.healthcheck.set_test("wget", {"port": values.network.web_port.port_number, "path": "/api/auth/providers"}) %}

{% do c1.environment.add_env("NODE_ENV", "production") %}
{% do c1.environment.add_env("HOSTNAME", "0.0.0.0") %}
{% do c1.environment.add_env("PORT", values.network.web_port.port_number) %}
{% do c1.environment.add_env("DATABASE_URL", postgres.get_url("postgres")) %}
{% do c1.environment.add_env("NEXTAUTH_URL_INTERNAL", "http://%s:%d"|format(values.consts.splitpro_container_name, values.network.web_port.port_number)) %}
{% do c1.environment.add_env("POSTGRES_PASSWORD", values.splitpro.db_password) %}
{% do c1.environment.add_env("NEXTAUTH_SECRET", values.splitpro.nextauth_secret) %}
{% do c1.environment.add_env("NEXTAUTH_URL", values.splitpro.nextauth_url.rstrip("/")) %}

{% do c1.environment.add_user_envs(values.splitpro.additional_envs) %}

{% do c1.add_port(values.network.web_port) %}

{% do c1.add_storage("/app/uploads", values.storage.uploads) %}
{% do perm_container.add_or_skip_action("uploads", values.storage.uploads, perms_config) %}

{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
  {% do perm_container.add_or_skip_action(store.mount_path, store, perms_config) %}
{% endfor %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
  {% do postgres.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{% do tpl.portals.add(values.network.web_port) %}

{{ tpl.render() | tojson }}
