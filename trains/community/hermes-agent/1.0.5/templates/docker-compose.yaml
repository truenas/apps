{% set tpl = ix_lib.base.render.Render(values) %}

{% set c1 = tpl.add_container(values.consts.hermes_agent_container_name, "image") %}
{% do c1.set_user(0, 0) %}
{% do c1.add_caps(["CHOWN", "SETUID", "SETGID", "DAC_OVERRIDE"]) %}
{% do c1.set_shm_size_mb(values.hermes_agent.shm_size_mb) %}
{% do c1.set_command(["gateway", "run"]) %}
{% do c1.healthcheck.set_test("curl", {"port": values.network.gateway_port.port_number, "path": "/health"}) %}

{% do c1.environment.add_env("API_SERVER_ENABLED", true) %}
{# Bind to all interfaces so the published port is reachable; the server otherwise defaults to loopback inside the container. #}
{% do c1.environment.add_env("API_SERVER_HOST", "0.0.0.0") %}
{% do c1.environment.add_env("API_SERVER_PORT", values.network.gateway_port.port_number) %}
{% do c1.environment.add_env("API_SERVER_KEY", values.hermes_agent.gateway_key) %}
{% do c1.environment.add_env("GATEWAY_ALLOW_ALL_USERS", values.hermes_agent.allow_all_users_access) %}

{% do c1.environment.add_env("HERMES_DASHBOARD", values.network.dashboard_port.bind_mode != "") %}
{% if values.network.dashboard_port.bind_mode %}
  {% do c1.environment.add_env("HERMES_DASHBOARD_PORT", values.network.dashboard_port.port_number) %}
  {% do c1.environment.add_env("HERMES_DASHBOARD_INSECURE", values.hermes_agent.enable_insecure_dashboard_access) %}
  {% do tpl.portals.add(values.network.dashboard_port) %}
{% endif %}

{% do c1.environment.add_env("WEBHOOK_ENABLED", values.network.webhook_port.bind_mode != "") %}
{% if values.network.webhook_port.bind_mode %}
  {% do c1.environment.add_env("WEBHOOK_PORT", values.network.webhook_port.port_number) %}
{% endif %}

{% do c1.environment.add_user_envs(values.hermes_agent.additional_envs) %}

{% do c1.add_port(values.network.gateway_port) %}
{% do c1.add_port(values.network.dashboard_port) %}
{% do c1.add_port(values.network.webhook_port) %}

{% do c1.add_storage("/opt/data", values.storage.data) %}
{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
{% endfor %}

{{ tpl.render() | tojson }}
