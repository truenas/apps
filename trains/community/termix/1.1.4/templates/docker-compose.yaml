{% set tpl = ix_lib.base.render.Render(values) %}

{% set c1 = tpl.add_container(values.consts.termix_container_name, "image") %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perms_config = {"uid": values.consts.run_as_user, "gid": values.consts.run_as_group, "mode": "check"} %}

{% do c1.set_user(values.consts.run_as_user, values.consts.run_as_group) %}
{% do c1.healthcheck.set_custom_test(["CMD", "node", "-e",
  "require('http').get('http://localhost:30001/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))",
]) %}
{% do c1.environment.add_env("PORT", values.network.http_port.port_number) %}
{% if values.network.certificate_id %}
  {% do c1.environment.add_env("ENABLE_SSL", true) %}
  {% do c1.environment.add_env("SSL_PORT", values.network.https_port.port_number) %}
  {% do c1.environment.add_env("SSL_CERT_PATH", values.consts.ssl_cert_path) %}
  {% do c1.environment.add_env("SSL_KEY_PATH", values.consts.ssl_key_path) %}

  {% set cert = values.ix_certificates[values.network.certificate_id] %}
  {% do c1.configs.add("private", cert.privatekey, values.consts.ssl_key_path) %}
  {% do c1.configs.add("public", cert.certificate, values.consts.ssl_cert_path) %}

  {% do c1.add_port(values.network.https_port) %}
{% endif %}

{% do c1.environment.add_user_envs(values.termix.additional_envs) %}

{% do c1.add_port(values.network.http_port) %}

{% do c1.add_storage("/app/data", values.storage.data) %}
{% do perm_container.add_or_skip_action("data", values.storage.data, perms_config) %}

{% for store in values.storage.additional_storage %}
  {% do c1.add_storage(store.mount_path, store) %}
  {% do perm_container.add_or_skip_action(store.mount_path, store, perms_config) %}
{% endfor %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{% do tpl.portals.add(values.network.http_port, {"name": "HTTP"}) %}
{% if values.network.certificate_id %}
  {% do tpl.portals.add(values.network.https_port, {"name": "HTTPS", "scheme": "https"}) %}
{% endif %}

{{ tpl.render() | tojson }}
